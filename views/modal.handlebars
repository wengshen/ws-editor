<div style="padding:10px;" class="ws-box-size">
    <div style="font-size:26px; font-weight:100;">Modal Editor</div>
    <br/><br/>
    Result:
    <br/><br/>
    <textarea id="chart-result" class="ws-box-size" style="font-family:monospace;width:100%;height:400px"></textarea>
    <br/><br/>
    <button id="modal-btn" class="ws-imp-button">Create a chart</button>
</div>

<script>
    ws.ready(function () {
        ws.ModalEditor('modal-btn', {
           // features: 'import templates',
            allowDone: true,
            features: 'data templates customize',
            importer: {
                options: 'plugins csv json samples'
            }
        }, function (chart) {
            var html = chart.export.html(true);
            ws.dom.get('chart-result').value = html;
        });
    });

</script>